from django.urls import path
# from .views import ShippingMethodViewSet, CalculateShippingPriceView

urlpatterns = [
    # path('api/shipping-methods/', ShippingMethodViewSet.as_view({'get': 'list'})),  # List of all shipping methods
    # path('api/shipping-methods/<int:shipping_method_id>/price/', CalculateShippingPriceView.as_view(), name='calculate-shipping-price'),
]
